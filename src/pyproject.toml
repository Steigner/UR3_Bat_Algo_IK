[tool.poetry]
name = "bat algorithm"
version = "0.1.0"
description = ""
authors = ["Steigner"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.21.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
